<p style="color: green"><%= notice %></p>

<% content_for :title, "Items" %>

<h1>Items</h1>

<div id="items">
  <% @proposal.items.each do |item| %>
    <%= render item %>
    <p>
      <%= link_to "Show this item", proposal_item_path(@proposal, item) %>
    </p>
  <% end %>
</div>

<%= link_to "New item", new_proposal_item_path %>
